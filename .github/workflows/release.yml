name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write
  pages: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

env:
  JAVA_VERSION: '21'

jobs:
  # Reuse CI (lint + tests + e2e)
  ci:
    name: CI (lint, build+tests, e2e)
    uses: ./.github/workflows/ci.yml

  publish-library:
    name: Publish Library & Plugin
    runs-on: macos-latest
    needs: [ ci ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: ${{ env.JAVA_VERSION }}
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
      - name: Publish to MavenCentral
        run: ./gradlew publishAndReleaseToMavenCentral --no-configuration-cache
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyId: ${{ secrets.SIGNING_KEY_ID }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNING_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.GPG_KEY_CONTENTS }}
      - name: Publish to Gradle Plugin Portal
        run: ./gradlew :gradle-plugins:publishPlugins --no-configuration-cache
        env:
          GRADLE_PUBLISH_KEY: ${{ secrets.GRADLE_PUBLISH_KEY }}
          GRADLE_PUBLISH_SECRET: ${{ secrets.GRADLE_PUBLISH_SECRET }}

  deploy-site:
    name: Deploy GitHub Pages (Landing, API, Demo)
    runs-on: ubuntu-latest
    needs: [ publish-library ]
    permissions:
      pages: write
      id-token: write
      contents: read
    environment:
      name: github-pages
      url: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
    steps:
      - name: Checkout Sources
        uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: ${{ env.JAVA_VERSION }}
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      # 1. Build Site Root (MkDocs Material)
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Install MkDocs Material
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs-material
      - name: Build MkDocs Site
        run: mkdocs build --clean

      # 2. Bypass Jekyll on GitHub Pages
      - name: Add .nojekyll
        run: touch site/.nojekyll

      # 3. Add API Reference (Dokka)
      - name: Generate API Docs
        run: ./gradlew :parikshan:dokkaGeneratePublicationHtml
      - name: Prepare API Docs
        run: |
          mkdir -p site/api
          cp -r parikshan/build/dokka/html/* site/api/

      # 4. Add Demo Site (Wasm)
      - name: Build Wasm Demo
        run: ./gradlew :samples:multiplatform-showcase:composeApp:wasmJsBrowserDistribution
      - name: Prepare Demo Site
        run: |
          mkdir -p site/demo
          cp -r samples/multiplatform-showcase/composeApp/build/dist/wasmJs/productionExecutable/* site/demo/

      # 5. Deploy to GitHub Pages
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
